# Google Maps API Key - required for route data collection (backend)
# Get one at: https://console.cloud.google.com/apis/credentials
# Enable: Directions API
GOOGLE_MAPS_API_KEY=your_api_key_here

# Google Maps API Key for frontend (Places Autocomplete + Maps)
# Can use the same key or a separate key with HTTP referrer restrictions
# Enable: Maps JavaScript API, Places API
VITE_GOOGLE_MAPS_API_KEY=your_api_key_here

# Optional: Server port (default 3001)
PORT=3001

# Optional: Minimum collection interval in seconds (default 300 = 5 min). Reduces Directions API cost.
# Collection never runs more often than this, even if the job is set to 1 min.
# MIN_CYCLE_SECONDS=300

# Optional: Restrict address autocomplete to a country (ISO 2-letter code, e.g. us, gb)
# Leave empty to use browser locale. Frontend reads VITE_PLACES_COUNTRY.
# VITE_PLACES_COUNTRY=us

# Optional: Require login to use the app. Set a password to enable auth; leave unset for no auth.
# AUTH_PASSWORD=your_secret_password

# Optional: Session signing secret (defaults to AUTH_PASSWORD if set). Use a long random string in production.
# AUTH_SECRET=your_long_random_session_secret

# Optional: Google Sign-In (OAuth 2.0). Create OAuth client at https://console.cloud.google.com/apis/credentials
# Application type: Web application. Add authorized redirect URI: https://your-domain.com/api/auth/google/callback
# (For local dev: http://localhost:3001/api/auth/google/callback)
# GOOGLE_OAUTH_CLIENT_ID=your_oauth_client_id
# GOOGLE_OAUTH_CLIENT_SECRET=your_oauth_client_secret

# Optional: When frontend and backend are on different hosts. For local dev (npm run dev), the backend
# redirects to http://localhost:5173 after Google login when it sees requests to port 3001.
# Set explicitly if redirect after login goes to the wrong URL:
# FRONTEND_URL=http://localhost:5173
# BACKEND_URL=http://localhost:3001
# For production: BACKEND_URL=https://your-api.example.com, FRONTEND_URL=https://your-app.example.com
